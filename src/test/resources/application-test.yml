server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

app:
  services:
    hmpps-auth:
      base-url: ${hmpps-auth.base-url:http://localhost:8091}
    strengths-and-needs-api:
      base-url: ${san-api.base-url:http://localhost:8091}