server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

app:
  services:
    hmpps-auth:
      base-url: ${hmpps-auth.base-url:http://hmpps-auth:9090}/auth
